import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.projetodispositivosmoveisfront',
  appName: 'projetodispositivosmoveisfront',
  webDir: 'dist'
};

export default config;
